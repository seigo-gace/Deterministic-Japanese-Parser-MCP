FROM python:3.12-slim
WORKDIR /app
COPY . .
RUN pip install --no-cache-dir .
ENV DJPMCP_HTTP_HOST=0.0.0.0 \
    DJPMCP_HTTP_PORT=8765 \
    DJPMCP_HTTP_WORKERS=1
EXPOSE 8765
ENTRYPOINT ["djpmcp-http"]
