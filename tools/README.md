# Tools

All tools create reviewable files only. They do not write into `dictionaries/system/` automatically.

- `learner.py`: deterministic proposals from unresolved JSONL logs
- `expander.py`: alias proposals from bundled synonym groups
- `gold_generator.py`: Gold Corpus candidates from real logs
- `validator.py`: schema, regex, and Gold Corpus validation
